<plan>
  <phase>1</phase>
  <plan-number>02</plan-number>
  <type>execute</type>
  <name>Global layout shell and Nav component</name>
</plan>

<objective>
Create the global Nav component (wordmark + Tracks link) and wire it into the root layout. Establish the page-level layout pattern (max-width container, consistent padding) that all page components will use. Create the types/index.ts file with all TypeScript interfaces.

Traces to: PRD §5 Nav, CONVENTIONS.md, PRD §4 Data Model
</objective>

<context>
- PRD: .planning/specs/PRD.md
- Conventions: .planning/codebase/CONVENTIONS.md
- Architecture: .planning/codebase/ARCHITECTURE.md
- Depends on: Phase 1, Plan 01 (scaffold complete)
</context>

<tasks>
  <task type="auto">
    <name>Create TypeScript type definitions</name>
    <files>types/index.ts</files>
    <action>
      Create types/index.ts in the project root with:

        export interface Resource {
          title: string
          url: string
          type: 'video' | 'reading' | 'interactive'
        }

        export interface Lesson {
          id: string
          trackId: string
          stage: number
          title: string
          duration: string
          description: string
          skills: string[]
          resources: Resource[]
          checkpoint: string
        }

        export interface Track {
          id: string
          title: string
          description: string
          icon: string          // emoji or short string
          lessons: string[]     // ordered lesson IDs
          tickets: string[]     // ticket IDs
        }

        export interface Ticket {
          id: string
          trackId: string
          title: string
          context: string
          requirements: string[]
          stretchGoals: string[]
          definitionOfDone: string
          difficulty: 'starter' | 'intermediate' | 'advanced'
        }

      Note: isLocked is NOT in the Lesson interface — it is derived at runtime.
    </action>
    <verify>
      - [ ] types/index.ts exists
      - [ ] All 4 interfaces present: Resource, Lesson, Track, Ticket
      - [ ] isLocked is NOT in Lesson interface
      - [ ] No TypeScript errors (run: npx tsc --noEmit)
    </verify>
    <done>types/index.ts created with all interfaces</done>
  </task>

  <task type="auto">
    <name>Create Nav component</name>
    <files>components/Nav.tsx</files>
    <action>
      Create components/Nav.tsx:

        'use client'

        import Link from 'next/link'
        import { usePathname } from 'next/navigation'

        export function Nav() {
          const pathname = usePathname()

          return (
            <nav className="border-b border-subtle bg-base">
              <div className="mx-auto max-w-5xl px-4 sm:px-6 flex items-center justify-between h-14">
                <Link
                  href="/"
                  className="text-primary font-bold text-lg tracking-tight hover:text-accent transition-colors"
                >
                  MAC Learn
                </Link>
                <div className="flex items-center gap-6">
                  <Link
                    href="/tracks"
                    className={`text-sm font-medium transition-colors ${
                      pathname.startsWith('/tracks')
                        ? 'text-accent'
                        : 'text-muted hover:text-primary'
                    }`}
                  >
                    Tracks
                  </Link>
                </div>
              </div>
            </nav>
          )
        }

      This uses usePathname (client hook) so the component is 'use client'.
      Active state: accent colour when on /tracks/*.
    </action>
    <verify>
      - [ ] components/Nav.tsx exists
      - [ ] Uses 'use client' directive
      - [ ] Links to / and /tracks
      - [ ] Uses only custom Tailwind tokens (no hardcoded hex, no bg-white)
    </verify>
    <done>Nav component created</done>
  </task>

  <task type="auto">
    <name>Wire Nav into root layout</name>
    <files>app/layout.tsx</files>
    <action>
      Update app/layout.tsx to:
        - Import Nav from '@/components/Nav'
        - Render Nav above {children}
        - Wrap everything in a flex column that fills min-h-screen

        import type { Metadata } from 'next'
        import { Geist } from 'next/font/google'
        import './globals.css'
        import { Nav } from '@/components/Nav'

        const geist = Geist({ subsets: ['latin'] })

        export const metadata: Metadata = {
          title: 'MAC Learn',
          description: 'Backend curriculum for MAC members — build practical skills step by step.',
        }

        export default function RootLayout({
          children,
        }: {
          children: React.ReactNode
        }) {
          return (
            <html lang="en">
              <body className={`${geist.className} bg-base text-primary min-h-screen`}>
                <Nav />
                <main>{children}</main>
              </body>
            </html>
          )
        }
    </action>
    <verify>
      - [ ] Nav is rendered in layout
      - [ ] html has lang="en"
      - [ ] body has bg-base and text-primary classes
      - [ ] No TypeScript errors
    </verify>
    <done>Layout renders Nav above page content</done>
  </task>

  <task type="auto">
    <name>Create reusable page container utility class pattern</name>
    <files>app/globals.css</files>
    <action>
      Add a reusable container layer class to globals.css so all pages use consistent max-width and padding:

        @layer components {
          .page-container {
            @apply mx-auto max-w-5xl px-4 sm:px-6 py-10;
          }
        }

      Page components will use:
        <div className="page-container">...</div>

      This enforces consistent layout without repeating Tailwind utility chains on every page.
    </action>
    <verify>
      - [ ] .page-container defined in globals.css @layer components
      - [ ] Dev server still starts without error
    </verify>
    <done>page-container utility available</done>
  </task>

  <task type="auto">
    <name>Final build verification</name>
    <files>none</files>
    <action>
      Run:
        npx tsc --noEmit    → should produce zero errors
        npm run build       → should complete with no errors

      If build fails due to missing /tracks page (dynamic routes), create stub:
        app/tracks/page.tsx:
          export default function TracksPage() { return <div className="page-container"><p className="text-muted">Coming soon</p></div> }
    </action>
    <verify>
      - [ ] npx tsc --noEmit passes
      - [ ] npm run build passes
    </verify>
    <done>Phase 1 complete — clean build, Nav in layout, types defined</done>
  </task>
</tasks>

<verification>
  - [ ] types/index.ts exists with all 4 interfaces
  - [ ] components/Nav.tsx renders wordmark and Tracks link
  - [ ] Nav active state works (accent on /tracks/*)
  - [ ] app/layout.tsx wires Nav into every page
  - [ ] .page-container utility defined
  - [ ] npx tsc --noEmit passes
  - [ ] npm run build passes
</verification>

<success_criteria>
Phase 1 complete: the project has a clean build, a working Nav in the global layout, all TypeScript types defined, and consistent layout utilities. All subsequent phases can focus on building features.
</success_criteria>

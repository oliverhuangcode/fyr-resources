# Phase 4, Plan 02 — Ticket Detail Page + TicketCard

## Goal

Implement `TicketCard`, `MarkTicketComplete`, and the full ticket detail page at `/tickets/[id]`. Also update the track detail page to use `TicketCard` instead of the raw link.

## Tasks

<task id="1">
Create `components/TicketCard.tsx`.

Server-compatible (no `'use client'`). Props: `ticket: Ticket` (import type from `@/types`).

Renders a card that links to `/tickets/{ticket.id}`:
- `<Link href={...}>` wrapping the whole card
- `bg-surface border border-subtle rounded-lg p-5 hover:border-accent transition-colors`
- Small label "Ticket" (`text-muted text-xs uppercase tracking-wide mb-1`)
- Ticket title (`text-primary font-medium`)
- Difficulty badge: `starter` → muted style, `intermediate` → accent-bordered, `advanced` → accent text

Difficulty badge classes:
- All: `text-xs border rounded px-2 py-0.5`
- `starter`: `border-subtle text-muted`
- `intermediate`: `border-accent text-accent`
- `advanced`: `border-accent text-accent font-semibold`
</task>

<task id="2">
Create `components/MarkTicketComplete.tsx`.

**CoVe required** (same pattern as MarkLessonComplete).

Client Component (`'use client'`). Props: `ticketId: string`.

CoVe Stage 1 — Write code [UNVERIFIED]:
- Import `useProgress`
- Derive `completed = isTicketCompleted(ticketId)`, use `hydrated` guard
- Placeholder while not hydrated (same as MarkLessonComplete)
- Completed state: "✓ Completed" in `text-accent`
- Button: `completeTicket(ticketId)` on click, same button style as MarkLessonComplete

CoVe Stage 2 — Verification targets:
1. `hydrated` guard prevents SSR flash
2. `completeTicket` is idempotent
3. State updates on click without page reload
4. Unknown ticketId does not crash

CoVe Stage 3 — Verify each:
1. `!hydrated` returns placeholder — ✓ safe
2. `useProgress.completeTicket`: `setCompletedTickets(prev => { if (prev.includes(ticketId)) return prev; ... })` — idempotent ✓
3. `setCompletedTickets` triggers re-render, `isTicketCompleted` returns true next render ✓
4. `isTicketCompleted` is array inclusion check — no crash ✓

CoVe Stage 4 — [VERIFIED]: proceed to implementation.
</task>

<task id="3">
Create `app/tickets/[id]/page.tsx`.

Server Component. `generateStaticParams()` returns all ticket IDs.

```ts
export function generateStaticParams() {
  return tickets.map(t => ({ id: t.id }))
}
```

Props: `{ params: { id: string } }`.

Lookup: find ticket by `params.id`; if not found, call `notFound()`. Look up track by `ticket.trackId`.

Layout (top to bottom):
1. Breadcrumb: `← {track.title}` link to `/tracks/{ticket.trackId}`, same style as lesson page
2. Header row: ticket title (`text-3xl font-bold text-primary`) + difficulty badge (same as TicketCard badge styles)
3. Context section — heading "Context", paragraph `text-muted`
4. Requirements section — heading "Requirements", `ol` (ordered list) with each requirement as `li`, `text-primary text-sm`, numbered style
5. Stretch Goals section — heading "Stretch Goals" (`text-muted`), `ul` with each goal as `li text-muted text-sm` with bullet
6. Definition of Done section — `bg-surface border border-subtle rounded-lg p-5`, heading "Definition of Done" inside, `text-muted` text
7. `<MarkTicketComplete ticketId={ticket.id} />` at the bottom

Wrap in `<div className="mx-auto max-w-3xl px-4 sm:px-6 py-12">`.
</task>

<task id="4">
Update `app/tracks/[trackId]/page.tsx` to use `TicketCard` instead of the inline ticket link block.

Replace the tickets section that renders a raw `<Link>` block with `<TicketCard ticket={ticket} />` for each ticket.

Import `TicketCard` from `@/components/TicketCard`. Keep the `tickets` import to look up ticket objects. The null-guard for missing tickets stays (though all ticket IDs in data will resolve).
</task>

## Verification

- [ ] `npx tsc --noEmit` passes
- [ ] `npm run build` passes — `/tickets/[id]` statically generated
- [ ] `generateStaticParams` present in ticket page
- [ ] TicketCard shows title and difficulty badge
- [ ] MarkTicketComplete shows placeholder, updates on click
- [ ] Ticket page shows context, requirements (ordered), stretch goals, DoD block
- [ ] Track detail page now uses TicketCard
- [ ] Breadcrumb links back to track from both lesson and ticket pages
- [ ] No hardcoded hex, no bg-white, no bg-gray-*
